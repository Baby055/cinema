aws:
  s3:
    bucket: dummy-bucket
  ses:
    source: dummy-ses-source

spring:
  datasource:
    url: jdbc:h2:mem:cinema;MODE=PostgreSQL
    username: sa
    password: sa
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false